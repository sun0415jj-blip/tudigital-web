# 티유디지털 / KCC HomeCC 프로젝트 개발자 가이드

> 작성일: 2026-07-03  
> 대상: 신규 합류 개발자, 유지보수 담당자

---

## 목차

1. [전체 프로젝트 개요](#1-전체-프로젝트-개요)
2. [프로젝트 A — 티유디지털 웹사이트 (tudigital-web)](#2-프로젝트-a--티유디지털-웹사이트-tudigital-web)
3. [프로젝트 B — KCC HomeCC 어드민 (kcc-admin)](#3-프로젝트-b--kcc-homec-어드민-kcc-admin)
4. [공통 개발 환경 세팅](#4-공통-개발-환경-세팅)
5. [배포 프로세스](#5-배포-프로세스)
6. [주요 외부 서비스 연동 정보](#6-주요-외부-서비스-연동-정보)
7. [트러블슈팅 히스토리](#7-트러블슈팅-히스토리)

---

## 1. 전체 프로젝트 개요

| 구분 | 프로젝트명 | 역할 | 기술 스택 | 배포 |
|---|---|---|---|---|
| A | tudigital-web | 티유디지털 구독신청 웹사이트 | 순수 HTML/CSS/JS + Google Apps Script | GitHub Pages / 정적호스팅 |
| B | kcc-admin | KCC HomeCC 파트너 어드민 | FastAPI + PostgreSQL | Railway |

두 프로젝트는 **완전히 독립적**으로 운영되며 공유 인프라 없음.

---

## 2. 프로젝트 A — 티유디지털 웹사이트 (tudigital-web)

### 2-1. 디렉토리 구조

```
tudigital-web/
├── index.html          # 메인 랜딩페이지
├── apply.html          # 구독 신청 폼
├── admin.html          # 관리자 어드민 (고객 목록, 서류 관리)
├── upload.html         # 고객용 서류 업로드 페이지 (링크 공유용)
├── agent.html          # 설계사용 고객 신청 페이지
├── partner.html        # 파트너 문의 페이지
├── subscribe.html      # 구독 안내 페이지
├── finance.html        # 금융 정보 페이지
├── history.html        # 이력 페이지
├── bundle.html         # 번들 상품 페이지
├── kcc.html            # KCC 연계 페이지
├── lg.html             # LG 연계 페이지
├── marketing.html      # 마케팅 페이지
└── Code.gs             # Google Apps Script 백엔드 (Google Sheets 연동)
```

### 2-2. 아키텍처

```
[고객 브라우저]
     │
     ├─ apply.html / agent.html ──→ [Google Apps Script (Code.gs)]
     │                                       │
     ├─ upload.html ──→ [ImgBB API]          │
     │                    (이미지 저장)       │
     │                       │               │
     │                  URL 반환             ▼
     │                       └──────→ [Google Sheets DB]
     │                                       │
     └─ admin.html ←─────────────────────────┘
          (관리자 조회/다운로드)
```

### 2-3. 백엔드: Google Apps Script (Code.gs)

**역할**: Google Sheets를 DB로 사용하는 서버리스 백엔드

**배포 위치**: Google Apps Script 웹앱으로 배포  
**접근 방식**: HTTP GET/POST 요청 (CORS 자유)

#### 주요 액션 (GET 파라미터 `action`으로 분기)

| action | 방식 | 설명 |
|---|---|---|
| *(없음)* | GET | 전체 고객 목록 반환 |
| `saveAllDocUrls` | GET | 서류 ImgBB URL 3개 저장 |
| `proxyImage` | GET | ImgBB 이미지 CORS 우회 다운로드 |

#### 주요 액션 (POST body `action`으로 분기)

| action | 설명 |
|---|---|
| `save` | 신규 고객 저장 + 담당자 이메일 알림 |
| `update` | 고객 정보 수정 |
| `delete` | 고객 삭제 |
| `uploadOneFile` | 파일 1개를 Google Drive에 업로드 |
| `contact` | 파트너 문의 이메일 발송 |

#### Apps Script URL 업데이트 방법
```
1. script.google.com → 프로젝트 열기
2. Code.gs 수정 후 저장 (Ctrl+S)
3. 배포 → 배포 관리 → 새 버전 배포
4. ※ URL은 변경되지 않음 (동일 배포 ID 유지)
```

#### admin.html / upload.html에서 URL 위치
```javascript
// admin.html, upload.html 상단 상수 선언부
const SCRIPT_URL = 'https://script.google.com/macros/s/...../exec';
const IMGBB_KEY  = '1bf49204aa251a5fae9ff0f169901c8f';
```

### 2-4. 파일 저장: ImgBB

- **사용처**: 고객 서류 이미지 (등기부등본, 신분증, 통장사본)
- **업로드 흐름**: 브라우저 → ImgBB API → URL 반환 → Google Sheets에 저장
- **제약사항**: ImgBB는 CORS 정책으로 인해 직접 fetch 다운로드 불가 → 새 탭 열기로 처리

### 2-5. Google Sheets DB 구조

| 컬럼 | 설명 |
|---|---|
| id | 고객 고유 ID (타임스탬프 기반) |
| name | 고객 이름 |
| birth | 생년월일 |
| phone | 연락처 |
| product | 상품명 |
| amount | 금액 |
| period | 구독기간 |
| debit | 자동이체일 |
| status | 상태 (서류대기 / 서류제출 / 확인완료) |
| confirmed | 서류확인 여부 (true/false) |
| createdAt | 신청일시 |
| docs | JSON 문자열 `{"doc1":"url","doc2":"url","doc3":"url"}` |

### 2-6. 어드민 인증

- 비밀번호: `tu2025!` (admin.html 내 `ADMIN_PASSWORD` 상수)
- 별도 서버 인증 없음, 클라이언트 로컬 검증

### 2-7. 성능 캐시 (Code.gs)

- Google Apps Script `CacheService` 사용
- 고객 목록 60초 캐시 (`customers_v1` 키)
- 데이터 변경 시 자동 무효화

---

## 3. 프로젝트 B — KCC HomeCC 어드민 (kcc-admin)

### 3-1. 디렉토리 구조

```
kcc-admin/
├── backend/
│   ├── main.py             # FastAPI 앱 진입점, 라우터 등록, 정적파일 서빙
│   ├── database.py         # DB 연결 (PostgreSQL/SQLite 자동 전환)
│   └── routers/
│       ├── auth.py         # 로그인 / 토큰 검증
│       ├── customers.py    # 고객 CRUD, 엑셀/CSV 업로드·다운로드
│       ├── estimates.py    # 견적서 관리
│       ├── api_keys.py     # 외부연동 API 키 관리 (신규)
│       └── leads.py        # 외부 랜딩페이지 고객 수신 API (신규)
├── frontend/
│   ├── index.html          # 어드민 SPA (단일 HTML 파일)
│   ├── landing.html        # 고객용 랜딩페이지
│   ├── estimate_customer.html    # 고객용 견적 확인 페이지
│   ├── estimate_subscription.html # 구독 견적 페이지
│   ├── sub_calculator.html # 구독 계산기
│   ├── vilaz.html          # VILAZ 브랜드 페이지
│   └── static/             # 이미지, CSS
├── Dockerfile
├── railway.toml
├── requirements.txt
├── run.py                  # 서버 실행 진입점
├── kcc_admin.db            # SQLite DB (로컬 개발용)
└── API_USAGE_GUIDE.md      # 외부 개발자용 연동 가이드
```

### 3-2. 아키텍처

```
[어드민 브라우저]
      │
      │  Bearer Token (30일 유효)
      ▼
[FastAPI 서버 — Railway]
      │
      ├─ /api/auth/*         → 로그인, 토큰 발급
      ├─ /api/customers/*    → 고객 CRUD (인증 필요)
      ├─ /api/estimates/*    → 견적서 CRUD (인증 필요)
      ├─ /api/channels/*     → API 키 관리 (인증 필요)
      └─ /api/v1/leads       → 외부 랜딩페이지 수신 (API Key 인증)
              │
              ▼
      [PostgreSQL — Railway]
      ├─ customers 테이블
      ├─ estimates 테이블
      └─ api_channels 테이블
```

### 3-3. 기술 스택

| 항목 | 내용 |
|---|---|
| 언어 | Python 3.11 |
| 웹 프레임워크 | FastAPI 0.111.0 |
| WSGI 서버 | Uvicorn 0.29.0 |
| DB (운영) | PostgreSQL (Railway 관리형) |
| DB (로컬) | SQLite (`kcc_admin.db`) |
| 엑셀 처리 | openpyxl, xlrd |
| 컨테이너 | Docker (python:3.11-slim) |
| 배포 플랫폼 | Railway |

### 3-4. DB 이중화 구조 (database.py)

`DATABASE_URL` 환경변수 유무로 자동 전환:

```python
DATABASE_URL = os.environ.get('DATABASE_URL') or os.environ.get('KCC_DB_URL')
IS_PG = bool(DATABASE_URL)

if IS_PG:
    # PostgreSQL 사용 (Railway 운영 환경)
else:
    # SQLite 사용 (로컬 개발 환경)
```

**로컬 개발 시**: 환경변수 없이 실행하면 자동으로 `kcc_admin.db` SQLite 사용  
**운영 환경**: Railway `DATABASE_URL` 환경변수가 자동 주입됨

> ⚠️ SQLite는 `?` 플레이스홀더, PostgreSQL은 `%s` 사용.  
> `database.py`의 `_PgConn.execute()`가 `?` → `%s` 자동 변환 처리하므로  
> 라우터 코드에서는 `?`만 사용해도 양쪽 동작.

### 3-5. DB 테이블 스키마

#### customers

| 컬럼 | 타입 | 설명 |
|---|---|---|
| id | SERIAL / INTEGER | PK |
| name | TEXT | 고객명 |
| phone | TEXT | 연락처 |
| addr | TEXT | 주소 |
| route | TEXT | 유입경로 (채널명 자동 매핑) |
| product | TEXT | 상품 (기본: 창호) |
| partner | TEXT | 파트너명 |
| stage | TEXT | 진행단계 (DB접수/방문배정/계약체결/시공진행/잔금완료/무효) |
| contract_type | TEXT | 계약유형 (구독/일시불/카드/할부) |
| contract_amount | INTEGER | 계약금액 |
| label | TEXT | 라벨 (일반/긴급/중복/부재중 등) |
| memo | TEXT | 메모 |
| measure_date | TEXT | 실측일 |
| construction_date | TEXT | 시공일 |
| contract_date | TEXT | 계약일 |
| receive_date | TEXT | 접수일 |
| checklists | TEXT | JSON 체크리스트 |
| created_at | TIMESTAMP | 등록일시 |

#### estimates

| 컬럼 | 타입 | 설명 |
|---|---|---|
| id | SERIAL / INTEGER | PK |
| customer_name | TEXT | 고객명 |
| customer_phone | TEXT | 연락처 |
| items | TEXT | JSON 견적 항목 |
| cost_total | INTEGER | 원가 합계 |
| quote_total | INTEGER | 견적 합계 |
| final_total | INTEGER | 최종 합계 |
| margin_rate | REAL | 마진율 (기본 16%) |
| created_at | TIMESTAMP | 작성일시 |

#### api_channels (신규)

| 컬럼 | 타입 | 설명 |
|---|---|---|
| id | SERIAL / INTEGER | PK |
| channel_name | TEXT | 채널명 (당근마켓, 유튜브 등) |
| api_key | TEXT UNIQUE | 발급된 API Key (`kcc_` 접두사) |
| is_active | BOOLEAN | 활성 여부 |
| created_at | TIMESTAMP | 발급일시 |

### 3-6. 인증 방식

#### 어드민 로그인 (JWT-like 자체 서명 토큰)

```
POST /api/auth/login  { "password": "..." }
→ { "token": "타임스탬프.HMAC서명" }
```

- 토큰 유효기간: **30일**
- 저장 위치: `localStorage('kcc_token')`
- 모든 API 요청 헤더: `Authorization: Bearer {token}`

#### 외부 API 인증 (API Key)

```
POST /api/v1/leads
Header: x-api-key: kcc_발급된키
```

- 어드민 [외부연동 API] 메뉴에서 채널별 발급
- `api_channels` 테이블에서 검증

### 3-7. 외부연동 API (신규)

#### POST /api/v1/leads — 외부 랜딩페이지 고객 등록

```
처리 흐름:
1. x-api-key 헤더 검증 (api_channels 테이블 조회)
2. is_active 확인
3. 연락처 기준 90일 이내 중복 체크
4. customers 테이블 INSERT (route = 채널명 자동 매핑)
```

**90일 중복 쿼리 (PostgreSQL)**
```sql
SELECT id FROM customers
WHERE phone = %s
  AND created_at > NOW() - INTERVAL '90 days'
LIMIT 1;
```

### 3-8. 고객 데이터 백업

```
GET https://admin.kcc-interior.com/api/customers/export/csv
```

전체 고객 데이터를 CSV로 다운로드. 브라우저에서 직접 접근 가능 (로그인 상태 필요).

### 3-9. 프론트엔드 구조 (index.html)

**단일 HTML SPA** 방식. 모든 페이지가 `index.html` 내 `<div class="page">` 섹션으로 구성.

```javascript
// 페이지 전환 함수
goPage('dashboard')   // DB 대시보드
goPage('customers')   // 고객 관리
goPage('db-input')    // 고객 DB 입력
goPage('contracts')   // 계약 관리
goPage('estimates')   // 견적 관리
goPage('converter')   // 견적 변환기
goPage('calendar')    // 시공 캘린더
goPage('api-keys')    // 외부연동 API 관리 (신규)
```

**디자인 토큰 (CSS 변수)**
```css
--accent: #1D9E75      /* 메인 그린 */
--bg: #fff             /* 카드 배경 */
--bg2: #f5f5f4         /* 사이드바 배경 */
--t1: #1a1a1a          /* 본문 텍스트 */
--t2: #6b6b6b          /* 보조 텍스트 */
--bd: #e0dedd          /* 구분선 */
```

---

## 4. 공통 개발 환경 세팅

### 4-1. 로컬 개발 — tudigital-web

별도 서버 불필요. HTML 파일을 브라우저에서 직접 열거나 Live Server 사용.

```bash
# VSCode Live Server 확장으로 실행
# 또는
npx serve .
```

> ⚠️ Google Apps Script 연동은 로컬에서도 실제 배포된 URL로 통신함.  
> Apps Script 코드 수정 시 반드시 **배포 → 새 버전**으로 재배포 필요.

### 4-2. 로컬 개발 — kcc-admin

```bash
# 1. Python 가상환경 생성
python -m venv venv
source venv/bin/activate      # Windows: venv\Scripts\activate

# 2. 의존성 설치
pip install -r requirements.txt

# 3. 서버 실행 (SQLite 자동 사용)
python run.py

# 접속: http://localhost:8000
# 어드민 비밀번호: 환경변수 ADMIN_PASSWORD (미설정 시 기본값 'tu4455')
```

**PostgreSQL 연결 테스트 시**
```bash
# .env 파일 생성 (Railway Postgres 연결)
DATABASE_URL=postgresql://user:pass@host:port/dbname
```

---

## 5. 배포 프로세스

### 5-1. tudigital-web 배포

```bash
git add .
git commit -m "변경 내용"
git push origin main
# → 정적 호스팅 자동 배포 (GitHub Pages 또는 연결된 CDN)
```

**Apps Script 배포는 별도**:
1. script.google.com → Code.gs 수정
2. 배포 → 배포 관리 → 새 버전 배포

### 5-2. kcc-admin 배포 (Railway)

```bash
git add .
git commit -m "변경 내용"
git push origin master
# → Railway 자동 빌드 & 배포 (Dockerfile 기반)
```

**배포 소요시간**: 약 1~3분

**Railway 환경변수 (Variables 탭)**

| 변수명 | 설명 |
|---|---|
| `DATABASE_URL` | Railway PostgreSQL 연결 문자열 (자동 주입) |
| `ADMIN_PASSWORD` | 어드민 로그인 비밀번호 |
| `ENV` | `production` 설정 시 reload 비활성화 |
| `PORT` | 포트 (Railway 자동 설정) |

> ⚠️ 환경변수 변경 후 반드시 **Deploy 버튼 클릭** 필요.  
> 변수 저장만으로는 반영되지 않음.

### 5-3. Railway 서비스 구성

```
zippy-prosperity (Railway 프로젝트)
├── kcc_admin      ← FastAPI 앱 (GitHub 연동, master 브랜치)
│   └── 커스텀 도메인: admin.kcc-interior.com
└── Postgres       ← Railway 관리형 PostgreSQL
    └── DATABASE_URL 자동 주입
```

---

## 6. 주요 외부 서비스 연동 정보

### 6-1. tudigital-web

| 서비스 | 용도 | 위치 |
|---|---|---|
| Google Apps Script | 백엔드 API / Sheets DB | Code.gs |
| Google Sheets | 고객 데이터 저장 | Apps Script 연결 스프레드시트 |
| ImgBB | 서류 이미지 저장 | `IMGBB_KEY` 상수 |
| MailApp (Gmail) | 서류접수 이메일 알림 | Code.gs `MANAGER_EMAIL` |
| 솔라피 (Solapi) | SMS/카카오 알림톡 | Code.gs `SOLAPI_*` 상수 |

### 6-2. kcc-admin

| 서비스 | 용도 | 비고 |
|---|---|---|
| Railway | 앱 호스팅 + PostgreSQL | admin.kcc-interior.com |
| 글로싸인 | 전자계약서 | 외부 링크 연동 |

---

## 7. 트러블슈팅 히스토리

### [tudigital-web] Apps Script 타임아웃 (2026-07)

- **증상**: 어드민 로드 시 `AbortError: signal is aborted` 콘솔 에러, 고객 목록 미표시
- **원인**: fetch 타임아웃 10초 설정 → Apps Script 콜드 스타트 시 초과
- **해결**: 타임아웃 30초로 증가 + CacheService 60초 캐시 적용으로 재현율 감소

### [tudigital-web] ImgBB 다운로드 불가 (2026-07)

- **증상**: 서류보기 모달에서 다운로드 버튼 클릭해도 동작 안 함
- **원인**: ImgBB CORS 정책으로 `fetch()` 실패, Apps Script 프록시도 실패
- **해결**: `downloadDoc()` 함수 제거, `<a target="_blank">` 직접 링크로 교체

### [tudigital-web] docs JSON 파싱 오류 (2026-07)

- **증상**: 서류를 제출한 고객임에도 "재업로드 후 다운로드 가능" 표시
- **원인**: Apps Script에서 docs 필드를 JSON 문자열로 반환하는 경우 클라이언트에서 파싱 실패
- **해결**: 로드 시 `typeof docs === 'string'` 체크 후 `JSON.parse()` 처리 추가

### [kcc-admin] Railway PostgreSQL 연결 실패 (2026-06)

- **증상**: 배포 후 어드민 접속 시 빈 대시보드
- **원인**: `DATABASE_URL` 환경변수가 `{{Postgres.DATABASE_URL}}` (Railway 템플릿 문법, `$` 없음)으로 잘못 설정됨
- **해결**: Postgres 서비스에서 실제 연결 문자열 복사 → kcc_admin Variables에 직접 붙여넣기 → Deploy 클릭

### [kcc-admin] SSL 인증서 오류 (2026-06)

- **증상**: admin.kcc-interior.com 접속 시 SSL 경고
- **원인**: 커스텀 도메인 DNS 레코드 미설정
- **해결**: Railway 커스텀 도메인 재추가 + DNS CNAME/TXT 레코드 업데이트

---

## 부록 — API 엔드포인트 전체 목록 (kcc-admin)

### 인증

| 메서드 | 경로 | 설명 |
|---|---|---|
| POST | `/api/auth/login` | 로그인, 토큰 반환 |

### 고객 관리 (인증 필요)

| 메서드 | 경로 | 설명 |
|---|---|---|
| GET | `/api/customers` | 고객 목록 (검색/필터) |
| POST | `/api/customers` | 고객 등록 |
| PUT | `/api/customers/{id}` | 고객 수정 |
| DELETE | `/api/customers/{id}` | 고객 삭제 |
| POST | `/api/customers/upload` | 엑셀/CSV 일괄 업로드 |
| GET | `/api/customers/export/csv` | 전체 고객 CSV 다운로드 |

### 견적서 (인증 필요)

| 메서드 | 경로 | 설명 |
|---|---|---|
| GET | `/api/estimates` | 견적서 목록 |
| POST | `/api/estimates` | 견적서 생성 |
| GET | `/api/estimates/{id}` | 견적서 상세 |
| PUT | `/api/estimates/{id}` | 견적서 수정 |
| DELETE | `/api/estimates/{id}` | 견적서 삭제 |

### 외부연동 API 키 관리 (인증 필요)

| 메서드 | 경로 | 설명 |
|---|---|---|
| GET | `/api/channels` | 채널 목록 |
| POST | `/api/channels` | 채널 생성 + API Key 발급 |
| PATCH | `/api/channels/{id}/toggle` | 활성/비활성 토글 |
| DELETE | `/api/channels/{id}` | 채널 삭제 |

### 외부 랜딩페이지 연동 (API Key 인증)

| 메서드 | 경로 | 설명 |
|---|---|---|
| POST | `/api/v1/leads` | 외부 고객 DB 등록 |

### 기타

| 메서드 | 경로 | 설명 |
|---|---|---|
| GET | `/health` | 헬스체크 (Railway용) |
| POST | `/api/landing/submit` | 기존 랜딩페이지 연동 (레거시) |
| GET | `/api/dbtest` | DB 연결 진단 |
